# 🚀 فاز ۱: طراحی پایهٔ معماری و ساختار توسعه‌پذیر پروژه React Geospatial Manager

> 🎯 **هدف کلان:** تبدیل پروژه به یک نرم‌افزار وب مقیاس‌پذیر و حرفه‌ای با ساختار ماژولار، ناوبری هوشمند و معماری تمیز برای توسعهٔ آینده.  
> این فاز پایهٔ پروژه را می‌سازد و هم‌زمان به‌صورت عملی اصول React، Routing، Context، و Layout را آموزش می‌دهد.

---

## 🧭 مرحله ۱ — تعریف ساختار پوشه‌ها و مسیرها (Project Skeleton)

### 🎯 هدف:
ایجاد ساختاری استاندارد و قابل گسترش برای نگهداری فایل‌ها و صفحات پروژه.

### 📂 ساختار پیشنهادی:
```
src/
├─ components/
├─ layouts/
├─ pages/
├─ routes/
├─ utils/
├─ assets/
├─ context/
├─ theme/
└─ App.jsx
```

### ⚙️ تسک‌ها:
1. ایجاد پوشه‌ها طبق ساختار بالا.
2. ساخت فایل `routes/AppRoutes.jsx` با محتوای زیر:
   ```jsx
   import { Routes, Route } from "react-router-dom";
   import MainLayout from "../layouts/MainLayout";
   import HomePage from "../pages/MainSite/HomePage";
   import ProjectNezamSakhteman from "../pages/NMCokhteman/ProjectNezamSakhteman";

   export default function AppRoutes() {
     return (
       <Routes>
         <Route element={<MainLayout />}>
           <Route path="/" element={<HomePage />} />
           <Route path="/nezam" element={<ProjectNezamSakhteman />} />
         </Route>
       </Routes>
     );
   }
   ```
3. در `App.jsx` فقط `AppRoutes` را ایمپورت و استفاده کن.

✅ **خروجی:**  
برنامه بدون خطا اجرا می‌شود و بین مسیرهای `/` و `/nezam` قابل جابه‌جایی است.

---

## 🧭 مرحله ۲ — طراحی Layout مرکزی (MainLayout)

### 🎯 هدف:
ساخت Layout مشترک برای همهٔ صفحات با قابلیت نمایش بخش‌های ثابت (Navbar, Footer).

### ⚙️ تسک‌ها:
1. فایل `layouts/MainLayout.jsx` را بساز:
   ```jsx
   import { Outlet } from "react-router-dom";
   import Navbar from "../components/Layout/Navbar";
   import Footer from "../components/Layout/Footer";

   export default function MainLayout() {
     return (
       <>
         <Navbar />
         <main style={{ padding: "1rem" }}>
           <Outlet />
         </main>
         <Footer />
       </>
     );
   }
   ```
2. در `Navbar.jsx` مسیرهای اصلی را با `NavLink` بساز:
   ```jsx
   import { NavLink } from "react-router-dom";

   export default function Navbar() {
     return (
       <nav>
         <NavLink to="/">صفحه اصلی</NavLink> |{" "}
         <NavLink to="/nezam">نظام مهندسی</NavLink>
       </nav>
     );
   }
   ```

✅ **خروجی:**  
صفحات داخل Layout مرکزی نمایش داده می‌شوند و ناوبری فعال است.

---

## 🧭 مرحله ۳ — افزودن Material UI

### 🎯 هدف:
استفاده از MUI برای طراحی رابط کاربری مدرن و واکنش‌گرا.

### ⚙️ تسک‌ها:
1. نصب پکیج‌ها:
   ```bash
   npm install @mui/material @emotion/react @emotion/styled
   ```
2. در Navbar از `AppBar`, `Toolbar`, `Button` استفاده کن.
3. ساختار پایهٔ تم را در `theme/theme.js` تنظیم کن.
4. فعال کردن `ThemeProvider` در `App.jsx`.

✅ **خروجی:**  
ظاهر پروژه حرفه‌ای و منطبق با اصول طراحی مدرن است.

---

## 🧭 مرحله ۴ — ساخت Context مرکزی (DataContext)

### 🎯 هدف:
ایجاد مرکز داده برای اشتراک‌گذاری اطلاعات بین صفحات.

### ⚙️ تسک‌ها:
1. ساخت فایل `context/DataContext.jsx`:
   ```jsx
   import { createContext, useState } from "react";

   export const DataContext = createContext();

   export function DataProvider({ children }) {
     const [projects, setProjects] = useState([]);

     return (
       <DataContext.Provider value={{ projects, setProjects }}>
         {children}
       </DataContext.Provider>
     );
   }
   ```
2. در `App.jsx` کل برنامه را درون `DataProvider` قرار بده.

✅ **خروجی:**  
اشتراک داده‌ها در سراسر برنامه فعال می‌شود (مبنای Data Management در فاز ۲).

---

## 🧭 مرحله ۵ — بهینه‌سازی و آماده‌سازی برای فاز ۲

### 🎯 هدف:
بهبود ساختار، تم، و آماده‌سازی برای مدیریت داده‌ها.

### ⚙️ تسک‌ها:
1. ایجاد `theme/theme.js` برای تعریف رنگ و فونت.  
2. افزودن Utility در `utils/` مثل:
   - `formatDate.js`
   - `geoUtils.js`
3. تنظیم `jsconfig.json` برای مسیرهای نسبی و تمیزتر کردن importها.

✅ **خروجی:**  
پروژه ساختاریافته، منظم و آماده برای ورود به فاز ۲ است.

---

## 🧩 جمع‌بندی نهایی فاز ۱
| بخش | هدف | وضعیت |
|------|------|---------|
| ساختار پروژه | ایجاد اسکلت حرفه‌ای و منظم | ✅ کامل |
| ناوبری | مسیرهای React Router | ✅ کامل |
| Layout | ثابت و قابل توسعه | ✅ کامل |
| Theme | آماده برای گسترش | ✅ فعال |
| Context | مدیریت داده مرکزی | ✅ فعال |
| آماده‌سازی فاز ۲ | زیرساخت Data Management | 🔜 بعدی |

---

📘 **فاز بعدی (فاز ۲):**
مدیریت داده‌ها (Import, Export, IndexedDB, TableView)

---

✍️ تهیه‌شده توسط: جعفر کیهانی  
📅 نسخه: 1.0 — مهر ۱۴۰۳  
